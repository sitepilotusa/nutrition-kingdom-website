# Handoff to the next run

First run. Start by building the snapshot and checking the basics: status codes for every
sitemap URL, canonical host consistency, structured data presence, duplicate titles.
